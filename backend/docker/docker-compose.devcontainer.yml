# Dev Container用のオーバーライド
# 開発時のみ適用される設定
services:
  backend:
    # 開発時は再起動しない（エディタ終了時にコンテナ停止）
    restart: "no"

    # 開発用環境変数（ベースのenvironmentを上書き）
    environment:
      ENVIRONMENT: development
      LOG_LEVEL: debug
      PYTHONPATH: /app
